import { Meta, Title, Description, Typeset, Story } from '@storybook/blocks';

<Meta title="Typography" />

<Title>Typography</Title>

Typography follows the scale defined in the design system. 

**Design reference:** [Figma (Typography)](https://www.figma.com/design/YbvOpVPUBxTSEzg4J8pXKu/Weni---unnnic?node-id=404-210&m=dev)

**Font:** Inter · **Weights:** 400 (Regular), 500 (Medium), 600 (Semibold)

Inter uses **letter-spacing** adjustments for legibility. **Prefer the typography mixins** so font and letter-spacing are always applied together; the variables remain available when you need them separately.

---

## Display

Headings and large titles. Use for page titles and section headers.

| Style      | Mixin (recommended) | Letter spacing | Size  | Line height | Weight |
|-----------|---------------------|----------------|-------|-------------|--------|
| Display 1 | `@include unnnic-font-display-1` | -4% | 24px  | 1.4        | Semibold   |
| Display 2 | `@include unnnic-font-display-2` | -4% | 20px  | 1.4        | Semibold   |
| Display 3 | `@include unnnic-font-display-3` | -2% | 16px  | 1.4        | Semibold   |
| Display 4 | `@include unnnic-font-display-4` | -2% | 16px  | 1.4        | Regular |

<Typeset
  fontSizes={[24, 20, 16]}
  fontWeight="600"
  sampleText="Display — Headings and section titles"
  fontFamily="Inter"
/>

<Typeset
  fontSizes={[16]}
  fontWeight="400"
  sampleText="Display 4 — Body large / lead text"
  fontFamily="Inter"
/>

---

## Body

Default text and emphasized content. Use for paragraphs, labels, and UI copy.

| Style   | Mixin (recommended) | Letter spacing | Size | Line height | Weight |
|---------|---------------------|----------------|------|-------------|--------|
| Body    | `@include unnnic-font-body`     | -1% | 14px | 1.4        | Regular |
| Emphasis| `@include unnnic-font-emphasis` | -1% | 14px | 1.4        | Medium  |
| Action  | `@include unnnic-font-action`   | -1% | 14px | 1.4        | Semibold    |

<Typeset
  fontSizes={[14]}
  fontWeight="400"
  sampleText="Body — Lorem ipsum dolor sit amet, consectetur adipiscing elit."
  fontFamily="Inter"
/>

<Typeset
  fontSizes={[14]}
  fontWeight="500"
  sampleText="Emphasis — Medium weight for highlighted text."
  fontFamily="Inter"
/>

<Typeset
  fontSizes={[14]}
  fontWeight="600"
  sampleText="Action — Semibold for buttons, labels, and CTAs."
  fontFamily="Inter"
/>

---

## Caption

Small text for captions, tags, and secondary information.

| Style     | Mixin (recommended) | Letter spacing | Size | Line height | Weight |
|-----------|---------------------|----------------|------|-------------|--------|
| Caption 1 | `@include unnnic-font-caption-1` | 0 | 12px | 1.4        | Medium  |
| Caption 2 | `@include unnnic-font-caption-2` | 0 | 12px | 1.4        | Regular |

<Typeset
  fontSizes={[12]}
  fontWeight="500"
  sampleText="Caption 1 — Tags and labels"
  fontFamily="Inter"
/>

<Typeset
  fontSizes={[12]}
  fontWeight="400"
  sampleText="Caption 2 — Secondary captions and hints"
  fontFamily="Inter"
/>

---

## Usage

**Recommended:** use the typography mixins so font and letter-spacing are always applied together.

**When the project injects unnnic via sass-loader** (e.g. rspack/webpack/vite with `additionalData: "@use '@weni/unnnic-system/src/assets/scss/unnnic.scss' as *;"`), the mixins are available in every SCSS file and Vue `<style lang="scss">` without adding `@use` in each file:

```scss
.my-heading {
  @include unnnic-font-display-1;
}
```

**When unnnic is not globally injected**, add the `@use` once at the top of the file:

```scss
@use '@weni/unnnic-system/src/assets/scss/unnnic.scss' as *;

.my-heading {
  @include unnnic-font-display-1;
}
```

If you need only the `font` or `letter-spacing` value (e.g. for overrides or calculations), use the variables: `$unnnic-font-display-1`, `$unnnic-font-letter-spacing-display-1`, etc.
